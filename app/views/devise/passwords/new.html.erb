<div class="banner-container">
  <div class="login-banner" style="background-image: url('<%= image_path "together2.jpg" %>');">
    <div class="banner-content">
      <h2>Forgotten your password?</h2>
      <div class="row">
        <div class="col-xs-12">
          <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <div class="form-inputs">
              <%= f.input :email, required: true, autofocus: true, placeholder: ("Enter here....") %>
            </div>
            <div class="form-actions text-center">
              <%= f.button :submit, "Reset Password",class: "btn btn-default btn-md", style:"color: #2DB94D ; font-weight: bold; width: 40%;" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
